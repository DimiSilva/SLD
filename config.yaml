version: 1

paths:
  tracks_root: docs/tracks
  guidelines_root: docs/guidelines
  examples_root: docs/examples
  custom_root: .sld/custom
  state_root: .sld
  templates_root: .sld/templates

naming:
  track_pattern: "<unix-timestamp-seconds>-name"
  slice_pattern: "<unix-timestamp-seconds>-name"
